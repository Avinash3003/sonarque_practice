[run]
# Use relative file paths so SonarCloud can correctly map coverage data
relative_files = True
# Measure coverage for the src/ directory (actual Python source files)
source = src

[report]
# Omit virtual environment and cache files
omit =
    .venv/*
    */__pycache__/*
    */site-packages/*
